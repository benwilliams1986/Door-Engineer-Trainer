# Equipment content

Create one folder per exact model. Keep all exact terminal data, parameter maps, manuals/references and lessons inside the model folder. Never mark model-specific content verified until checked against an authoritative manual for the relevant revision.
